# Libraries

library(data.table)  # fast data import + manipulation
library(dplyr)       # data wrangling (filter, mutate, joins)
library(tidyr)       # reshaping data (pivot_longer/wider)
library(ggplot2)     # visualisation
library(caret)       # modelling + cross-validation
library(corrplot)    # correlation plots
library(xgboost)     # gradient boosting models
library(cowplot)     # combine multiple ggplot charts